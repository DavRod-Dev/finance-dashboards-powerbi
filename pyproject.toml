[project]
name = "finance-dashboards-powerbi"
version = "0.1.0"
description = "Power BI dashboards over a reproducible star-schema export from three financial data repositories"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
